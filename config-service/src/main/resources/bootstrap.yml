
server:
  port: 8081
  error:
    include-message: always

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/diegoahumadasegurosalfa/ms-course-youtube.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "02-config"
        bootstrap: true
  security:
    user:
      name: root
      password: s3cr3t